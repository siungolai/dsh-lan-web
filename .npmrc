# dsh-lan-web dev install policy: peer packages (dsh-client-connection,
# dsh-client-ui-sidebar, react-dom, ...) are provided by the DSH web profile at
# runtime — skip auto-installing them locally to keep node_modules lean.
legacy-peer-deps=true
